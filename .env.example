# ---------------------------------------------------------------------------
# Database (Supabase Postgres via Prisma)
# ---------------------------------------------------------------------------
# Supabase: Project Settings -> Database -> Connection string
# Use the "Transaction" pooler (port 6543) for DATABASE_URL and the direct
# connection (port 5432) for DIRECT_URL (needed for migrations).
DATABASE_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:5432/postgres"

# ---------------------------------------------------------------------------
# Supabase (Auth + Storage)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# ---------------------------------------------------------------------------
# Razorpay
# ---------------------------------------------------------------------------
NEXT_PUBLIC_RAZORPAY_KEY_ID="rzp_test_xxxx"
RAZORPAY_KEY_SECRET="your-razorpay-key-secret"
RAZORPAY_WEBHOOK_SECRET="your-razorpay-webhook-secret"

# ---------------------------------------------------------------------------
# Resend (transactional email)
# ---------------------------------------------------------------------------
RESEND_API_KEY="re_xxxx"
RESEND_FROM_EMAIL="orders@shreemeenakshisweets.com"

# ---------------------------------------------------------------------------
# Twilio (WhatsApp order notifications) — optional, feature no-ops without it
# ---------------------------------------------------------------------------
TWILIO_ACCOUNT_SID="ACxxxx"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_WHATSAPP_FROM="+14155238886"

# ---------------------------------------------------------------------------
# Phone OTP login — currently delivered as an automated VOICE CALL via 2Factor.in
# (src/lib/twofactor.ts). Optional: until set, phone sign-in shows a friendly
# "not set up" message. Supabase's native phone-auth providers don't cover this,
# so the app places the call / verifies the code itself and bridges a verified
# number into a real Supabase session (see verifyPhoneOtpAction in
# src/app/(auth)/actions.ts). Set these in Vercel too, not just locally.
# ---------------------------------------------------------------------------
TWOFACTOR_API_KEY="your-2factor-api-key"
# Optional: secret used to sign the short-lived cookie tying a call to its phone
# number. Falls back to SUPABASE_SERVICE_ROLE_KEY.
OTP_SESSION_SECRET=""

# ---------------------------------------------------------------------------
# Cron (Vercel Cron -> /api/cron/cancel-stale-orders)
# ---------------------------------------------------------------------------
# Any random string — Vercel automatically sends it as a Bearer token when
# it triggers the cron; set the same value in the Vercel project settings.
CRON_SECRET="a-long-random-string"

# ---------------------------------------------------------------------------
# App
# ---------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Shree Meenakshi Sweets & Savouries"
